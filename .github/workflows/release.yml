name: Release
on:
  push:
    tags: ['[0-9]*.[0-9]*.[0-9]*']
permissions:
  contents: write
  id-token: write
  attestations: write
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
      - run: npm install --global npm@10.9.9
      - run: npm ci
      - run: npm audit --audit-level=moderate
      - run: npm run release:verify
        env:
          RELEASE_TAG: ${{ github.ref_name }}
      - uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            release/main.js
            release/manifest.json
            release/styles.css
      - name: Publish verified assets
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: gh release create "$RELEASE_TAG" --verify-tag --title "$RELEASE_TAG" --notes-file docs/RELEASE-NOTES.md release/main.js release/manifest.json release/styles.css
